{
  "name": "insert-dynamodb",
  "version": "1.0.0",
  "description": "A script which inserts some data into dynamodb for testing streams",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1.0",
    "uuid": "^8.3.2"
  }
}
